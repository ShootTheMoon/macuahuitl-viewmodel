# Macuahuitl v11 작업 기록 (2026-09-14)

요청 → 조치 → 결과 순서. 현재 상태 기준 수치는 `SPEC_v11_KO.md`.

---

## 1. 계획 — 모델 교체 + 양손 전환
**요청**: v10 완성본(`Macuahuitl_Rework_v10`)의 모델을 폴더의 새 모델로 교체하고 한손 → 양손 검으로 완전히 바꾸는 계획. 모델링·애니메이션은 Higgsfield AI Blender GUI MCP 로.

**결정 (질문 답변)**
- 모델 `Macuahuitl_TwoHand.glb`
- 애니메이션 하이브리드: v10 무기 궤적 유지 + 왼팔 IK, 연출 동작은 양손으로 전환
- RareDraw 양손 회전 투척
- 폴리곤 경량화 없음
- 결과는 복사본 폴더 `Macuahuitl_Rework_v11_TwoHand` (v10 은 건드리지 않음)

## 2. 연결 문제
**상황**: Blender 를 리셋해도 브리지(`bl_*`)가 응답 없음.
**원인**: Blender 안의 blender-mcp 애드온 서버와 Higgsfield 브리지가 둘 다 포트 9876 을 사용.
**조치**: blender-mcp 서버 정지 → `bpy.ops.higgsfield.restart_mcp()` → 정상.

## 3. v11.0 — 1차 전환
- 무기 크기: 게임 카메라 비교 렌더에서 사용자가 **0.8배(1.55 m)** 선택
- 3파트 분할 (Handle · Body · Obsidian), 그립 오른손 x 0.45 · 왼손 x 0.65
- Lua↔Blender 좌표 규약을 v10 씬으로 검증 (오차 0) — `v11_convention.py`
- 동작 32 · 전환 20 양손 전환, 막기 3종 새 자세, FBX · 매니페스트 · 계약 출력
- 영상 `Motions_v10_vs_v11.mp4` (사용자 요청 "영상으로 보여줘")

## 4. v11.1 — 무겁게 · 텍스처 렌더 · 변신 발광
**피드백**: 양손이니 무겁게(느리게). 솔리드 회색 말고 모델링 적용한 상태로 보여줘. 궁 → Transform → 클릭 → UltAttack 구조이니 변신 연출 필요 — **텍스처 문양이 빛나는 것**.
**조치**
- `v11_retime.py` 리타이밍 (예비 ×1.5 · 휘두르기 ×1.2 · 2f 멈춤 · 회수 ×1.4)
- 재질 렌더(EEVEE) 영상으로 전환
- `v11_glow.py` 문양 발광 (손잡이 쪽부터 칼끝까지 차오름, 강화 맥동, 타격 뒤 꺼짐)
- 버그: 손잡이 면 재질 번호 유실(구리색) 복원, 나무 노멀맵에 색 텍스처가 꽂혀 있던 것 해제
- 영상 `Motions_v11_heavy_glow.mp4`

## 5. v11.2 — 더 무겁게 · 두 손 RareDraw · 팔 끊김
**피드백**: RareDraw 를 한 손으로 돌리지 말고 두 손으로 무겁게. 전체적으로 더 무겁게. 변신은 잘 됨(승인). 팔 안 보여야 할 부분(잘린 끝)이 보여 팔이 떨어져 보임.
**조치**: 리타이밍 ×1.8/1.3/3f/1.7 + 무게 레이어, RareDraw 두 손 8자 휘돌리기, 위팔 메시를 어깨 쪽으로 연장.
- 영상 `Motions_v11_2_heavy.mp4`

## 6. v11.3 — 요청 재해석
**피드백**: "두 손을 떼지 말라"고 한 적 없음 — 한 손으로 너무 가볍게 던지던 걸 두 손으로 던지라는 것. 팔은 늘리지 말고 **기본 메시 그대로** 최대한 안 보이게.
**조치**: RareDraw 두 손 투척 + v10 부메랑 비행 복원, 연장 팔 메시 폐기, IK 어깨 오프셋을 카메라 가시성 기준으로 프레임마다 선택.
- 교훈: 모호한 피드백은 사용자가 말한 만큼만 바꾼다
- 영상 `Motions_v11_3.mp4`

## 7. v11.4 — 버서커 무게 · 물리
**피드백**: 11초 부근 팔 겹침. 3타가 내려치는데 손은 안 내려감. RareDraw 는 잡을 때만 두 손이고 실제로는 한 손으로 앞으로 던지는데 옆으로 날아감 → 옆 비행에 맞게 자연스럽게, 잡을 때도 두 손으로. 버서커처럼 더 묵직하게.
**조치**
- 왼손 놓는 구간 목표를 화면 아래로 (TUCK) → 겹침 해결
- arc+dip 레이어: 무기와 손이 같이 내려가고 몸이 가라앉음
- RareDraw 새로 제작: 칼날 수평으로 눕혀 두 손으로 옆으로 휘둘러 던짐 → 수평 회전 비행 → 두 손 캐치 → 무게에 끌림
- 히트스톱 4프레임, 예비 하중 10°, 밀고 나감 14°
- 영상 `Motions_v11_4.mp4` (최신), 체크포인트 v11_09

## 8. 임포트 준비
**요청**: "이제 임포트 준비". 결정 — 흑요석만 3만 삼각형 이하로 감축, 몸체 텍스처 + 손잡이 단색, 패키지+가이드만(임포트는 직접).
**조치**: `Import_OVERDARE/` 패키지 (FBX · 텍스처 · Lua 32 · 배치표 · 가이드), 흑요석 194,120 → 28,420 (평균 차이 0.5 mm).

**요청**: 팔까지 전부 교체 — 사실상 Macuahuitl 을 새로 넣는 것.
**조치**: 9파트 전부 파트별 FBX 로 재출력, 가이드를 전체 재임포트용으로 갱신.

## 9. 게임 반영
**요청**: "일단 메시 넣음 ㄱ" (사용자가 FBX 9개 임포트 완료).
**조치**: 기존 `Workspace.Macuahuitl_Viewmodel` 9 MeshPart 를 같은 GUID 로 교체 (위치·크기·색·재질·텍스처), 클립 모듈 32개 교체, 백업 `onlyoneshot_BEFORE_MACUAHUITL_V11_20260914.ovdrjm`.

## 10. 팔·무기가 게임에서 사라짐
**보고**: "팔 걍 사라짐" (+ `japan1_viewmodel.md` 는 팔 붙이는 방식 참고용). 확인 결과 팔·무기 둘 다 안 보임.
**원인**: 임포트 1회에 에셋이 STATIC_MESH 와 MODEL 두 개 등록되는데, MeshId 에 **MODEL id** 를 넣었음 — 에디터에선 보이지만 플레이 중엔 안 보임.
**조치**: 9파트 MeshId 를 STATIC_MESH id 로 교체 → 로그상 v10 과 같은 방식으로 로드.

## 11. 궁 쓰면 팔이 멈춤
**보고**: "궁쓰면 팔 멈춰버림".
**원인**: 컨트롤러 `updateUltMovement` 가 용 궁용 이동 방향 `ultMotion.dir` 를 곱하는데 maxico 궁엔 없음 → 2221행 에러가 매 프레임 → 자세 적용 전에 멈춤.
**조치**: 가드에 `and ultMotion.dir` 한 조건 추가. 백업 `onlyoneshot_BEFORE_ULTFIX_20260914.ovdrjm`.

## 12. 궁 써도 방망이가 변신 안 함
**보고**: "궁쓰면 변신해야되는데 안바뀜 방망이가".
**원인**: 게임에는 원래(v10 부터) 변신 시각 효과가 없었음. 발광은 Blender 영상에만 있던 효과.
**조치**
- 엔진이 런타임 색·투명도를 못 쓰므로, 문양 발광을 구운 텍스처 3장(채움 35/70/100%) 제작 → TEXTURE 임포트
- 레벨에 몸체 사본 `MAC_Body_Glow1~3` 추가 (처음엔 파일에 직접 넣었으나 `level.apply` 가 새 인스턴스를 무시 → Studio 에 실시간 생성, Size 형식 문제 해결 후 저장)
- 컨트롤러에 `MELEE.glowStage()` 와 몸체 교체 분기 추가
- 플레이테스트: 파트 12개 로드, 에러 0. 백업 `onlyoneshot_BEFORE_GLOW_20260914.ovdrjm`

## 13. 궁 쓸 때 화질이 너무 낮아짐
**보고**: "궁쓸때 퀄리티가 너무 낮아지는 버그".
**조사**: 로그에 화질 단계·해상도 변경 없음, 궁 화면 효과는 이름 글자 + 빨간 번쩍임뿐, 텍스처 임포트 설정은 원래 몸체와 동일 → 발광 몸체 쪽 원인.
**원인 (추정 2가지)**: ① 발광 몸체 재질이 Unlit 이라 조명·음영이 빠져 납작해 보임 ② 숨긴 몸체를 9,000 cm 밖으로 치워 텍스처가 저해상도로 내려감 → 꺼낼 때 흐림.
**조치**: 재질 Plastic, 숨김 위치를 카메라 바로 아래 400 cm 로. 플레이테스트 에러 0. 백업 `onlyoneshot_BEFORE_GLOWQUALITY_20260914.ovdrjm`.
**상태**: 사용자 확인 대기.

## 14. 정리
**요청**: 폴더에 명세서 전부 정리 + 지금까지 정보 + zip 을 바탕화면에.
**조치**: `SPEC_v11_KO.md` (명세서), 이 문서, `Game_Integration/` (게임 스크립트 현재본), 바탕화면 `Macuahuitl_Rework_v11_TwoHand_20260914.zip`.
